apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: 2300032080/skill14_cicd:latest   # ✅ Your image
        ports:
        - containerPort: 8080             # ✅ Must match app’s internal port
---
apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  ports:
    - port: 8085       # Cluster port
      targetPort: 8080  # Container port
      nodePort: 30018   # Accessible via http://localhost:30018
  selector:
    app: frontend
